"use strict";
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || function (mod) {
    if (mod && mod.__esModule) return mod;
    var result = {};
    if (mod != null) for (var k in mod) if (k !== "default" && Object.prototype.hasOwnProperty.call(mod, k)) __createBinding(result, mod, k);
    __setModuleDefault(result, mod);
    return result;
};
var __awaiter = (this && this.__awaiter) || function (thisArg, _arguments, P, generator) {
    function adopt(value) { return value instanceof P ? value : new P(function (resolve) { resolve(value); }); }
    return new (P || (P = Promise))(function (resolve, reject) {
        function fulfilled(value) { try { step(generator.next(value)); } catch (e) { reject(e); } }
        function rejected(value) { try { step(generator["throw"](value)); } catch (e) { reject(e); } }
        function step(result) { result.done ? resolve(result.value) : adopt(result.value).then(fulfilled, rejected); }
        step((generator = generator.apply(thisArg, _arguments || [])).next());
    });
};
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.start = exports.command = exports.help = void 0;
const fs_1 = require("fs");
const path_1 = require("path");
const chalk_1 = __importDefault(require("chalk"));
const superstruct_1 = require("superstruct");
const vite_1 = require("vite");
const PreviewOptionsStruct = (0, superstruct_1.object)({
    host: (0, superstruct_1.optional)((0, superstruct_1.boolean)()),
    open: (0, superstruct_1.optional)((0, superstruct_1.boolean)()),
    port: (0, superstruct_1.optional)((0, superstruct_1.union)([(0, superstruct_1.number)(), (0, superstruct_1.string)()]))
});
const { error, info } = console;
exports.help = (0, chalk_1.default) `
{blue email preview}

Starts the preview server for a directory of email templates

{underline Usage}
  $ email preview <template dir path> [...options]

{underline Options}
  --host      Allow thew preview server to listen on all addresses (0.0.0.0)
  --no-open   Do not open a browser tab when the preview server starts
  --port      The local port number the preview server should run on. Default: 55420

{underline Examples}
  $ email preview ./src/templates --port 55420
`;
const command = (argv, input) => __awaiter(void 0, void 0, void 0, function* () {
    if (input.length < 1)
        return false;
    (0, superstruct_1.assert)(argv, PreviewOptionsStruct);
    const [target] = input;
    const targetPath = (0, path_1.resolve)(target);
    yield (0, exports.start)(targetPath, argv);
    return true;
});
exports.command = command;
const start = (targetPath, argv) => __awaiter(void 0, void 0, void 0, function* () {
    if (!(0, fs_1.existsSync)(targetPath)) {
        error((0, chalk_1.default) `\n{red D'oh} The directory provided ({dim ${targetPath}}) doesn't exist`);
        return;
    }
    const { host = false, open = true, port = 55420 } = argv;
    const { default: config } = yield Promise.resolve().then(() => __importStar(require('../../app/vite.config')));
    const mergedConfig = Object.assign(Object.assign({ configFile: false }, config), { resolve: {
            alias: {
                '@': targetPath
            }
        }, server: { host, port: parseInt(port, 10) } });
    const server = yield (0, vite_1.createServer)(mergedConfig);
    info((0, chalk_1.default) `\n  🚀 {yellow JSX email} Preview\n`);
    yield server.listen();
    // TODO: bind CLI shortcuts. this hasn't landed in Vite yet, will be released with 5.0
    if (open)
        server.openBrowser();
    server.printUrls();
});
exports.start = start;
