"use strict";
var __awaiter = (this && this.__awaiter) || function (thisArg, _arguments, P, generator) {
    function adopt(value) { return value instanceof P ? value : new P(function (resolve) { resolve(value); }); }
    return new (P || (P = Promise))(function (resolve, reject) {
        function fulfilled(value) { try { step(generator.next(value)); } catch (e) { reject(e); } }
        function rejected(value) { try { step(generator["throw"](value)); } catch (e) { reject(e); } }
        function step(result) { result.done ? resolve(result.value) : adopt(result.value).then(fulfilled, rejected); }
        step((generator = generator.apply(thisArg, _arguments || [])).next());
    });
};
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.command = exports.help = void 0;
const promises_1 = require("node:fs/promises");
const path_1 = require("path");
const chalk_1 = __importDefault(require("chalk"));
const mustache_1 = __importDefault(require("mustache"));
const superstruct_1 = require("superstruct");
const { log } = console;
const CreateOptionsStruct = (0, superstruct_1.object)({
    jsx: (0, superstruct_1.optional)((0, superstruct_1.boolean)()),
    out: (0, superstruct_1.optional)((0, superstruct_1.string)())
});
exports.help = (0, chalk_1.default) `
{blue email create}

Creates a new jsx-email template

{underline Usage}
  $ email create <template name> [...options]

{underline Options}
  --jsx   Use a JSX template instead of TSX. TSX is the default
  --out   The directory to create the new template in. Defaults to the current directory.

{underline Examples}
  $ email create invite
  $ email create invite --out=src/assets
`;
const command = (argv, input) => __awaiter(void 0, void 0, void 0, function* () {
    if (input.length < 1)
        return false;
    (0, superstruct_1.assert)(argv, CreateOptionsStruct);
    const [name] = input;
    const { jsx, out } = argv;
    const template = yield (0, promises_1.readFile)((0, path_1.join)(__dirname, '../assets/email.mustache'), 'utf8');
    const data = {
        name,
        propsType: jsx ? '' : ': TemplateProps',
        typeInfer: jsx ? '' : ', type Infer',
        typeProps: jsx ? '' : '\nexport type TemplateProps = Infer<typeof TemplateStruct>;'
    };
    const newContent = mustache_1.default.render(template, data);
    const outPath = (0, path_1.resolve)(out || process.cwd());
    const outFile = `${name}.tsx`;
    log('Creating a new template at', outPath);
    yield (0, promises_1.mkdir)(outPath, { recursive: true });
    yield (0, promises_1.writeFile)((0, path_1.join)(outPath, outFile), newContent);
    log(`Template ${outFile} created`);
    return true;
});
exports.command = command;
