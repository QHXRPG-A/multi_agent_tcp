#!/usr/bin/env node
"use strict";
var __awaiter = (this && this.__awaiter) || function (thisArg, _arguments, P, generator) {
    function adopt(value) { return value instanceof P ? value : new P(function (resolve) { resolve(value); }); }
    return new (P || (P = Promise))(function (resolve, reject) {
        function fulfilled(value) { try { step(generator.next(value)); } catch (e) { reject(e); } }
        function rejected(value) { try { step(generator["throw"](value)); } catch (e) { reject(e); } }
        function step(result) { result.done ? resolve(result.value) : adopt(result.value).then(fulfilled, rejected); }
        step((generator = generator.apply(thisArg, _arguments || [])).next());
    });
};
var __rest = (this && this.__rest) || function (s, e) {
    var t = {};
    for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p) && e.indexOf(p) < 0)
        t[p] = s[p];
    if (s != null && typeof Object.getOwnPropertySymbols === "function")
        for (var i = 0, p = Object.getOwnPropertySymbols(s); i < p.length; i++) {
            if (e.indexOf(p[i]) < 0 && Object.prototype.propertyIsEnumerable.call(s, p[i]))
                t[p[i]] = s[p[i]];
        }
    return t;
};
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
const debug_1 = __importDefault(require("debug"));
const import_local_1 = __importDefault(require("import-local"));
const yargs_parser_1 = __importDefault(require("yargs-parser"));
const package_json_1 = __importDefault(require("../package.json"));
const build_1 = require("./commands/build");
const create_1 = require("./commands/create");
const help_1 = require("./commands/help");
const preview_1 = require("./commands/preview");
const commands = { build: build_1.command, create: create_1.command, help: help_1.command, preview: preview_1.command };
const debug = (0, debug_1.default)('@jsx-email/cli');
const { log } = console;
const run = () => __awaiter(void 0, void 0, void 0, function* () {
    const argv = (0, yargs_parser_1.default)(process.argv.slice(2));
    const { _: positionals } = argv, flags = __rest(argv, ["_"]);
    const [commandName] = positionals;
    let command = commands[commandName];
    debug(`Command Name: \`${commandName}\``);
    if (flags.version) {
        log(`${package_json_1.default.name} v${package_json_1.default.version}\n`);
        return;
    }
    if (!command)
        command = help_1.command;
    const input = positionals.slice(1) || [];
    const result = yield command(flags, input);
    if (!result) {
        debug(`Command \`${commandName}\` returned \`false\``);
        (0, help_1.command)({}, []);
    }
});
if ((0, import_local_1.default)(__filename)) {
    debug('Using local install of webpack-command');
}
else {
    run();
}
