"use strict";
var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __name = (target, value) => __defProp(target, "name", { value, configurable: true });
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);

// src/stringify-styles.ts
var stringify_styles_exports = {};
__export(stringify_styles_exports, {
  stringifyStyles: () => stringifyStyles
});
module.exports = __toCommonJS(stringify_styles_exports);
var import_escape_string = require("./escape-string.js");
var UPPERCASE = /([A-Z])/g;
var MS = /^ms-/;
var UNITLESS_PROPS = /* @__PURE__ */ new Set([
  "animationIterationCount",
  "columns",
  "columnCount",
  "flex",
  "flexGrow",
  "flexShrink",
  "fontWeight",
  "gridColumn",
  "gridColumnEnd",
  "gridColumnStart",
  "gridRow",
  "gridRowEnd",
  "gridRowStart",
  "lineHeight",
  "opacity",
  "order",
  "orphans",
  "tabSize",
  "widows",
  "zIndex",
  "zoom"
]);
var CACHE = {};
function hyphenateChar(char) {
  return `-${char.toLowerCase()}`;
}
__name(hyphenateChar, "hyphenateChar");
function hyphenateString(prop) {
  return prop.replace(UPPERCASE, hyphenateChar).replace(MS, "-ms-");
}
__name(hyphenateString, "hyphenateString");
function stringifyStyles(styles) {
  const parts = [];
  for (const key of Object.keys(styles)) {
    const value = styles[key];
    if (value != null) {
      const unit = typeof value === "number" && value !== 0 && !UNITLESS_PROPS.has(key) ? "px" : "";
      const normalized = CACHE[key] || (CACHE[key] = hyphenateString(key));
      parts.push(`${normalized}:${typeof value === "number" ? value : (0, import_escape_string.escapeString)(value)}${unit}`);
    }
  }
  return parts.join(";");
}
__name(stringifyStyles, "stringifyStyles");
// Annotate the CommonJS export names for ESM import in node:
0 && (module.exports = {
  stringifyStyles
});
/**
 * Note: Parts of this file are derived from [Hyperons](https://github.com/i-like-robots/hyperons).
 * @license MIT
 */
//# sourceMappingURL=stringify-styles.js.map