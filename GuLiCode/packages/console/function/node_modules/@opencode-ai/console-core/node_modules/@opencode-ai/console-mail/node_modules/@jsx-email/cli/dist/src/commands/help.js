"use strict";
var __awaiter = (this && this.__awaiter) || function (thisArg, _arguments, P, generator) {
    function adopt(value) { return value instanceof P ? value : new P(function (resolve) { resolve(value); }); }
    return new (P || (P = Promise))(function (resolve, reject) {
        function fulfilled(value) { try { step(generator.next(value)); } catch (e) { reject(e); } }
        function rejected(value) { try { step(generator["throw"](value)); } catch (e) { reject(e); } }
        function step(result) { result.done ? resolve(result.value) : adopt(result.value).then(fulfilled, rejected); }
        step((generator = generator.apply(thisArg, _arguments || [])).next());
    });
};
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.command = exports.helpMessage = void 0;
const chalk_1 = __importDefault(require("chalk"));
const package_json_1 = __importDefault(require("../../package.json"));
const build_1 = require("./build");
const create_1 = require("./create");
const preview_1 = require("./preview");
const { log } = console;
exports.helpMessage = (0, chalk_1.default) `
{blue ${package_json_1.default.name}} v${package_json_1.default.version}

${package_json_1.default.description}

{underline Usage}
  $ email [...options]

{underline Commands}
  build       {dim <template path>}
  create      {dim <template name>}
  help        [{dim <command>}]
  preview     {dim <template dir path>}

{underline Options}
  --help      Displays this message
  --version   Displays the current @jsx-email/cli version

{underline Examples}
  $ email
  $ email --help
  $ email build ./src/templates/Invite.tsx
  $ email create invite
  $ email preview ./src/templates
`;
const commands = { build: build_1.help, create: create_1.help, preview: preview_1.help };
const command = (_, inputs) => __awaiter(void 0, void 0, void 0, function* () {
    if ((inputs || []).length < 1) {
        log(exports.helpMessage);
        return true;
    }
    const [command] = inputs;
    const commandHelp = commands[command] || exports.helpMessage;
    log(commandHelp);
    return true;
});
exports.command = command;
