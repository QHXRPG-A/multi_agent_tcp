"use strict";
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || function (mod) {
    if (mod && mod.__esModule) return mod;
    var result = {};
    if (mod != null) for (var k in mod) if (k !== "default" && Object.prototype.hasOwnProperty.call(mod, k)) __createBinding(result, mod, k);
    __setModuleDefault(result, mod);
    return result;
};
var __awaiter = (this && this.__awaiter) || function (thisArg, _arguments, P, generator) {
    function adopt(value) { return value instanceof P ? value : new P(function (resolve) { resolve(value); }); }
    return new (P || (P = Promise))(function (resolve, reject) {
        function fulfilled(value) { try { step(generator.next(value)); } catch (e) { reject(e); } }
        function rejected(value) { try { step(generator["throw"](value)); } catch (e) { reject(e); } }
        function step(result) { result.done ? resolve(result.value) : adopt(result.value).then(fulfilled, rejected); }
        step((generator = generator.apply(thisArg, _arguments || [])).next());
    });
};
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.command = exports.help = void 0;
const node_fs_1 = require("node:fs");
const promises_1 = require("node:fs/promises");
const node_os_1 = __importDefault(require("node:os"));
const path_1 = require("path");
const doiuse_email_1 = require("@jsx-email/doiuse-email");
const render_1 = require("@jsx-email/render");
const chalk_1 = __importDefault(require("chalk"));
const cheerio_1 = require("cheerio");
const esbuild_1 = __importDefault(require("esbuild"));
const globby_1 = __importDefault(require("globby"));
const html_minifier_terser_1 = require("html-minifier-terser");
const js_beautify_1 = __importDefault(require("js-beautify"));
const superstruct_1 = require("superstruct");
const { error, log } = console;
const BuildOptionsStruct = (0, superstruct_1.object)({
    check: (0, superstruct_1.optional)((0, superstruct_1.boolean)()),
    minify: (0, superstruct_1.optional)((0, superstruct_1.boolean)()),
    out: (0, superstruct_1.optional)((0, superstruct_1.string)()),
    plain: (0, superstruct_1.optional)((0, superstruct_1.boolean)()),
    pretty: (0, superstruct_1.optional)((0, superstruct_1.boolean)()),
    props: (0, superstruct_1.optional)((0, superstruct_1.string)()),
    strip: (0, superstruct_1.optional)((0, superstruct_1.boolean)())
});
const emailClients = [
    'apple-mail.*',
    'gmail.*',
    'outlook.*',
    'protonmail.*',
    'hey.*',
    'fastmail.*'
];
exports.help = (0, chalk_1.default) `
{blue email build}

Builds a template and saves the result

{underline Usage}
  $ email build <template path> [...options]

{underline Options}
  --check       Check built templates for client compatibility
  --minify      Minify the rendered template before saving
  --no-strip    Prevents stripping data-id attributes from output
  --out         File path to save the rendered template
  --plain       Emit template as plain text
  --pretty      Oututs HTML in a pretty-print format. Note: Don't use this for production.
  --props       A JSON string containing props to be passed to the email template
                This is usually only useful when building a single template, unless all of your
                templates share the same props.

{underline Examples}
  $ email build ./src/emails
  $ email build ./src/templates/Invite.tsx
  $ email build ./src/templates/Invite.tsx --props='\{"batman": "Bruce Wayne"\}'
`;
const prettify = (html) => {
    const defaults = {
        indent_char: ' ',
        indent_inner_html: true,
        indent_size: 2,
        sep: '\n',
        unformatted: ['code', 'pre', 'em', 'strong', 'span']
    };
    return js_beautify_1.default.html(html, defaults);
};
// Credit: https://github.com/rollup/plugins/blob/master/packages/pluginutils/src/normalizePath.ts#L5
const normalizePath = (filename) => filename.split(path_1.win32.sep).join(path_1.posix.sep);
const stripHtml = (html) => {
    const $ = (0, cheerio_1.load)(html, { xml: { decodeEntities: false }, xmlMode: true });
    $('*').removeAttr('data-id');
    return $.html();
};
const build = (path, argv) => __awaiter(void 0, void 0, void 0, function* () {
    const { minify, out, plain, pretty = false, props = '{}', strip = true } = argv;
    const template = yield Promise.resolve(`${path}`).then(s => __importStar(require(s)));
    const componentExport = template.Template || template.default;
    const extension = plain ? '.txt' : '.html';
    if (!componentExport) {
        error(`${path} does not contain a named \`Template\` or default export of a JSX Element`);
        process.exit(1);
    }
    const buildProps = JSON.parse(props);
    const component = componentExport(buildProps);
    const writePath = (0, path_1.join)(out, (0, path_1.basename)(path).replace((0, path_1.extname)(path), extension));
    if (plain) {
        const plainText = (0, render_1.render)(component, { plainText: plain });
        yield (0, promises_1.writeFile)(writePath, plainText, 'utf8');
        return plainText;
    }
    let html = (0, render_1.render)(component);
    if (strip)
        html = stripHtml(html);
    if (minify)
        html = yield (0, html_minifier_terser_1.minify)(html);
    if (pretty)
        html = prettify(html);
    yield (0, promises_1.mkdir)(out, { recursive: true });
    yield (0, promises_1.writeFile)(writePath, html, 'utf8');
    return html;
});
const compile = (files, outDir) => __awaiter(void 0, void 0, void 0, function* () {
    yield esbuild_1.default.build({
        bundle: true,
        entryPoints: files,
        logLevel: 'error',
        outdir: outDir,
        platform: 'node',
        write: true
    });
    return (0, globby_1.default)([normalizePath((0, path_1.join)(outDir, '*.js'))]);
});
const formatSubject = (what) => what.replace(/`([\s\w<>.-]+)`/g, (_, bit) => (0, chalk_1.default) `{bold ${bit}}`).trim();
const combine = (lines) => {
    const rePreamble = /(`([\s\w<>.-]+)`[\s\w]+)/;
    const result = lines.reduce((prev, curr) => {
        const matches = curr.match(rePreamble);
        const preamble = matches[0];
        prev[preamble] = (prev[preamble] || []).concat(curr.replace(rePreamble, '').replace(/`/g, ''));
        return prev;
    }, {});
    for (const key of Object.keys(result)) {
        result[key] = Array.from(new Set(result[key]));
    }
    return result;
};
const command = (argv, input) => __awaiter(void 0, void 0, void 0, function* () {
    var _a, _b;
    if (input.length < 1)
        return false;
    const [target] = input;
    const tmpdir = yield (0, promises_1.realpath)(node_os_1.default.tmpdir());
    const esbuildOutPath = (0, path_1.join)(tmpdir, 'jsx-email', Date.now().toString());
    const { check = false, plain } = argv;
    if (!(yield (0, node_fs_1.existsSync)(target))) {
        error((0, chalk_1.default) `{red The provided build target '${target}' does not exist}`);
        process.exit(1);
    }
    (0, superstruct_1.assert)(argv, BuildOptionsStruct);
    // Note: niave check that will probably get us into some edge cases
    const isFile = target.endsWith('.tsx') || target.endsWith('.jsx');
    const { out = '.rendered' } = argv;
    const glob = isFile ? target : (0, path_1.join)(target, '*.{jsx,tsx}');
    const targetFiles = yield (0, globby_1.default)([normalizePath(glob)]);
    const outputPath = (0, path_1.resolve)(out);
    log((0, chalk_1.default) `{cyan Found}`, targetFiles.length, 'files:');
    log('  ', targetFiles.join('\n  '));
    log((0, chalk_1.default) `\n{blue Starting build...}`);
    const compiledFiles = yield compile(targetFiles, esbuildOutPath);
    const builtFiles = yield Promise.all(compiledFiles.map((filePath, index) => __awaiter(void 0, void 0, void 0, function* () {
        return {
            fileName: targetFiles[index],
            html: yield build(filePath, Object.assign(Object.assign({}, argv), { out: outputPath }))
        };
    })));
    if (check && !plain) {
        log((0, chalk_1.default) `{blue Checking results for Client Compatibility...}\n`);
        const counts = { errors: 0, notes: 0, warnings: 0 };
        for (const file of builtFiles) {
            const result = (0, doiuse_email_1.doIUseEmail)(file.html, { emailClients });
            const { success } = result;
            // eslint-disable-next-line no-continue
            if (success && !result.warnings)
                continue;
            log((0, chalk_1.default) `{underline ${file.fileName}}\n`);
            if (!success && ((_a = result.errors) === null || _a === void 0 ? void 0 : _a.length)) {
                const errors = combine(result.errors);
                const indent = '           ';
                for (const [preamble, clients] of Object.entries(errors)) {
                    log((0, chalk_1.default) `  {red error}  ${formatSubject(preamble)}:\n${indent}{dim ${clients.join(`\n${indent}`)}}\n`);
                    counts.errors += 1;
                }
            }
            if ((_b = result.warnings) === null || _b === void 0 ? void 0 : _b.length) {
                const warnings = combine(result.warnings);
                const indent = '          ';
                for (const [preamble, clients] of Object.entries(warnings)) {
                    log((0, chalk_1.default) `  {yellow warn}  ${formatSubject(preamble)}:\n${indent}{dim ${clients.join(`\n${indent}`)}}\n`);
                    counts.warnings += 1;
                }
            }
            // FIXME: Think about how to intelligently display notes, because they aren't always useful
            // if (result.notes?.length) {
            //   const notes = combine(result.notes);
            //   const indent = '          ';
            //   for (const [preamble, clients] of Object.entries(notes)) {
            //     log(
            //       chalk`  {green note}  ${formatSubject(preamble)}:\n${indent}{dim ${clients.join(
            //         `\n${indent}`
            //       )}}\n`
            //     );
            //     counts.notes += 1;
            //   }
            // }
        }
        const errors = counts.errors > 0 ? chalk_1.default.red(counts.errors) : chalk_1.default.green(counts.errors);
        const warnings = counts.warnings > 0 ? chalk_1.default.yellow(counts.warnings) : chalk_1.default.green(counts.warnings);
        log((0, chalk_1.default) `{green Check Complete:} ${errors} error(s), ${warnings} warning(s)`);
    }
    log((0, chalk_1.default) `\n{green Build complete}. File(s) written to:`, outputPath);
    return true;
});
exports.command = command;
