# Frontend Archive

This folder contains GuLiCode desktop and app-side records:

1. Blueprint side panel and canvas UI.
2. Session composer integration for Top Agent planning.
3. Runtime status projection in the renderer.
4. Agent information panels, popout windows, CSS, i18n, and UI tests.

Use this folder when the next task touches `GuLiCode/packages/app`, desktop
renderer wiring, blueprint planning UX, or user-visible runtime controls.

